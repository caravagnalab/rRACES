set -x
set -e

which cmake


if [ ! -d RACES ]; then

    git clone https://github.com/albertocasagrande/RACES.git

    (cd RACES; git checkout 7b0a7f6 && rm -rf .git )

fi

${R_HOME}/bin/R --vanilla --silent -e 'Rcpp::compileAttributes()'
